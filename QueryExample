List<Opportunity> assign = [ SELECT Id, Name, CloseDate FROM Opportunity WHERE Account.Name like '%abc%'];
System.debug(assign);

for( Opportunity a : assign ){
    System.debug(a.Name + '/' +a.CloseDate );
}
